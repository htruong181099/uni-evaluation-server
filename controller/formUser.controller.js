const db = require("../model");
const FormUser = db.formUser;
const User

exports.addFormUser = async (req,res,next)=>{
    try {
        // FormUser.insertMany()
    } catch (error) {
        next(error);
    }
}
